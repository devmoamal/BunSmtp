version: "3.8"
services:
  smtp-relay:
    build: .
    container_name: bun-smtp-relay
    ports:
      - "${SMTP_PORT:-587}:587"
    environment:
      - SMTP_PORT=587
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_DOMAIN=${SMTP_DOMAIN:-localhost}
      - DEBUG=${DEBUG:-false}
    restart: always
